# Banco de dados

Schema mínimo de identidade e sessões. Não contém tabelas financeiras nesta etapa. Use somente `db:generate` e `db:migrate`; `push` é proibido fora de experimentos locais descartáveis.
