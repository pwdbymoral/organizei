# Módulos

`web` depende de `ui` e `database`; `domain` não depende de framework, HTTP, autenticação ou banco. Fiscalização de imports é executada pelo lint e será reforçada conforme surgirem fronteiras reais.
